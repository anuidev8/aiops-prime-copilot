/**
 * Canonical CopilotKit agent ids used by frontend tool registrations.
 * Must match the server runtime agent key (`CopilotRuntime.agents.default`).
 */
export const CHAT_COPILOT_AGENT_ID = "default";

